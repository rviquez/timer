{
  "name": "timer",
  "version": "1.0.0",
  "description": "pomodoro timer",
  "main": "index.js",
  "scripts": {
    "start": "npm run lite",
    "lite": "lite-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "roberto viquez",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.3.0"
  }
}
